import React from 'react';
import Link from 'next/link';
import { Film, Gamepad, FolderRoot as Football } from 'lucide-react';

// ... [rest of the existing Home component code remains exactly the same]
